<h1>Showing selected article</h1>

<p>
	<%= @article.title %>
</p>

<p>
	<%= @article.description %>
</p>

<%= link_to "Edit", edit_article_path(@article), class: 'btn btn-default' %>
<%= link_to "Delete", article_path(@article), method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-danger' %><br>
<%= link_to "Show all articles", articles_path,  class: 'btn btn-default' %>

